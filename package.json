{
  "name": "photos",
  "private": true,
  "type": "module",
  "workspaces": ["core", "cli", "site"],
  "engines": { "node": ">=20" },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc -b"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
